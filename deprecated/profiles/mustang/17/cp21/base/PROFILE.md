# mustang-android17-cp21

Android 17 CP21 runtime test profile for Pixel 10 Pro XL / `mustang`.

Source evidence: `profiles/android17-pending/mustang/CP21.260330.011/thermal-files/`.

State: **test / runtime verification required**.

Patch rule: only existing `VIRTUAL-SKIN*` objects with `PollingDelay=300000` are changed to `5000`. No non-virtual-skin sensors are intentionally changed by this generated profile.
