# 2.0.0-alpha.3-dev.2 Mustang runtime evidence

Status date: `2026-07-26`.

This document records the exact-package installation and post-reboot runtime proof for the Alpha 3 next refactor. It is development evidence only and does not publish or promote an update channel.

## Package binding

- file: `pixel-10-thermal-memory-control-2.0.0-alpha.3-dev.2.zip`;
- SHA-256: `3590bf96d55fd577326b240301d660fffbdef48513bdbf73cc1305fdb1f6d13b`;
- size: `303616` bytes;
- version: `2.0.0-alpha.3-dev.2`;
- versionCode: `1016213`;
- tested V2 merge commit: `fe9adf225c7401cb73520705b60a945e88c44bac`.

The installer read the selected Magisk cache package directly and recorded the same SHA-256 and byte count.

## Device and selected configuration

- device: Pixel 10 Pro XL (`mustang`);
- Android: `17` / SDK `37`;
- build: `CP2A.260705.006`;
- incremental: `15641320`;
- fingerprint: `google/mustang/mustang:17/CP2A.260705.006/15641320:user/release-keys`;
- build evidence: `exact_verified`;
- Polling: Mod;
- Thermal: Stock, exact Outdoor delta `0`;
- ZRAM: enabled at 100p;
- pTune: installed-disabled, no active conflict;
- Debug: verbose;
- battery at install: `100` percent, no external power;
- installer elapsed time: `35` seconds.

## Installer and validation-state proof

The install completed successfully into `modules_update` and recorded:

- `install_menu_model=single_process_v1`;
- `install_menu_process_count=1`;
- `install_options_confirmed=1`;
- all five install choices were collected once;
- Thermal and ZRAM materializers ran noninteractively;
- three stock thermal files were processed;
- `22` PollingDelay values changed from `300000` to `5000`;
- Outdoor validation passed for 3 files, 2 target zones, 2 arrays, and 14 numeric values;
- canonical validation state was written under `/data/adb/pixel-10-pro-xl-thermal-fix/validation/`;
- `state.env`, `validation-report.json`, `outdoor-delta-validation.env`, and `patch-manifest.tsv` were present and hash-bound;
- historical validation paths were symlinks only.

## Post-reboot runtime PASS

The staged module was consumed and the active module reported the exact version and versionCode. The verifier completed with:

- `checks_failed=0`;
- `warnings=0`;
- final marker: `RESULT: CG_INSTALLED_RUNTIME_VERIFY_DONE outcome=success workflow_exit_code=0`.

Verified runtime details:

- module `disable`, `skip_mount`, and `remove` markers absent;
- dynamic source, source manifest, source cache, patch manifest, validation report, and materialization checks passed;
- all canonical validation-state hashes matched `install-state.txt` and `state.env`;
- all five historical paths resolved to the canonical files as symlinks;
- all three active `/vendor/etc` thermal file hashes matched the module overlays;
- Stock exact delta remained `0` with the expected 3/2/2/14 validation counts;
- active Polling validation passed;
- Bootguard pending absent, fail count `0`, threshold `2`, last-good present;
- ZRAM swap size was approximately 100 percent of RAM and `/dev/block/zram0` was active;
- active ZRAM algorithm was `lz77eh`;
- no ZRAM service failure marker was present;
- pTune remained installed-disabled and no staged pTune update existed;
- Action contained no network refresh path and included performance instrumentation;
- Thermal service was responsive;
- no recent fatal Thermal pattern was detected.

## Evidence boundary

This proves the exact Alpha 3 dev.2 package and refactor on Mustang with an exact-evidence build. It does not yet satisfy the separate public Alpha 3 gate requiring successful installation or Action rematerialization on at least one unlisted supported-platform build using `dynamic_unverified` local stock validation.

Stable `update.json`, public prerelease `update-prerelease.json`, tags, releases, and public assets remain unchanged.
