# 2.0.1

2.0.1 is a focused Dynamic V2 hotfix for newer Pixel 10 Thermal files and includes the full 2.0.0 stable feature set.

## 2.0.1 hotfix

- Accepts both compact and multiline `HotThreshold` arrays during independent Outdoor-delta validation.
- Keeps exact per-value delta checks, target-zone matching, sentinel preservation, controlled-file scope, and fail-closed behavior unchanged.
- Fixes the false `rc=63` install rejection seen after the August Thermal file formatting change.

## Device verification

The hotfix code was verified on Pixel 10 Pro XL (`mustang`) running Android 17:

- August Stable `CP2A.260805.005` on KernelSU: install passed, full post-reboot Bootguard/runtime verification passed, all three active `/vendor/etc` Thermal files matched their validated overlays, Polling Mod was active at `22/22` values of `5000` with no remaining controlled `300000` values, and ZRAM 100% was active.
- August Canary on `mustang`: independently verified by Allen Chang.
- July Stable `CP2A.260705.006` on Magisk: regression install validation remained green with all `22/22` Polling replacements and all 12 target zones / 84 threshold values validated.

Thanks to **mOjO jOjO** and **Allen Chang** for the fast August validation.

## Included 2.0.0 stable changelog

The following 2.0.0 baseline changes remain included in 2.0.1.

### Dynamic thermal architecture

- Builds guarded overlays from each device's own stock Thermal configuration.
- Admits supported Pixel 10 codenames on Android 17 and validates monthly or Canary builds locally.
- Limits modifications to the admitted Polling and Outdoor targets in three controlled Thermal files.
- Fails closed when source structure, exact deltas, manifests, active mounts, or runtime evidence do not validate.
- Quarantines stale overlays and rematerializes them after firmware or platform transitions.

### Thermal controls

- Polling Mode can preserve stock values or change admitted `PollingDelay` values from `300000` to `5000`.
- Thermal profiles include Stock, Outdoor Safe, Outdoor Plus, and Outdoor Extended.
- The fixed five-choice Action menu applies and persists all four profiles plus Back.

### Memory controls

- Optional ZRAM sizing at approximately 100% of physical RAM with `lz77eh` where supported and `vm.swappiness=100`.
- Adaptive Emerald Hill operation for daily use plus a separately acknowledged experimental maximum-frequency minimum lock.
- Optional LMKD 1% swap-free policy with Magisk `resetprop` first, readback verification, fallback handling, targeted reload, and bounded evidence.
- Independent pTune conflict detection and an explicit high-risk coexistence override.

### Boot and recovery

- Bootguard records pending and signed last-good state and can disable or skip the overlay after bounded failures.
- Install, update, firmware/configuration changes, pending transitions, debug mode, or invalid evidence trigger full verification.
- Unchanged verified normal boots can use the lightweight path while retaining automatic escalation.
- Magisk P/T/Z/L badges refresh without a permanent watcher.
- Module removal cleans both the Magisk module path and persistent module state.

### 2.0.0 device verification baseline

The exact 2.0.0 package passed on a Pixel 10 Pro XL (`mustang`) running Android 17 build `CP2A.260705.006` with full first-boot verification, active validated vendor overlays, `22/22` Polling values at `5000`, Outdoor Extended validation across 12 target zones / 84 threshold values, ZRAM 100%, successful LMKD property write and targeted AOSP reinit, and green P/T/Z/L manager badges.

### Repository verification

The 2.0.0 release-candidate workflows covered the lean package contract, installer state, Dynamic V2 admission, Thermal and Outdoor transactions, ZRAM and Emerald Hill, LMKD, Bootguard, pTune, Action routing, and full/light boot behavior.

## Scope

This release does not add Pixel 10a support. `stallion` / Tensor G4 support remains a separate device-family validation task.

## Stable asset

- Asset: `pixel-10-thermal-memory-control-2.0.1.zip`
- SHA-256: `6517cd106acd063e52596d4fc0f2e561cd019cdaa3712e930fcddaf746d4dbaa`
- Size: `330935` bytes
- Package entries: `56`
- Stable update channel: `2.0.1` / versionCode `1016241`
