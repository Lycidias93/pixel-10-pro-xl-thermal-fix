# Pixel Thermal & Memory Control 2.1.0-alpha.9

This is a **public vNext prerelease**. Stable users can remain on 2.0.4; Alpha9 promotes the hardware-accepted Pixel 11 sparse-overlay path while retaining the Alpha7 recovery controls and conservative Tensor G6 timing policy.

## New in 2.1.0-alpha.9

- **Pixel 11 Thermal overlays are sparse.** On Tensor G6, only Thermal JSON files with a real admitted policy change are materialized under the module overlay.
- **Untouched stock Thermal files stay outside the overlay.** Unmodified G6 graph members are preserved byte-for-byte, including their original trailing-newline state, so formatting alone cannot create a false sparse-overlay diff.
- **Full-stock behavior is preserved.** Stock polling + Stock Thermal + Stock recovery still produces no Thermal JSON overlay at all.
- **Pixel 11 policy is unchanged.** Classic PollingDelay and PassiveDelay remain Stock-only; Outdoor Safe remains capped at +1 °C on the master `VIRTUAL-SKIN` definition in `thermal_info_config_common.json`, while same-named peripheral graph entries stay stock.
- **Recovery remains independent.** HotHysteresis and MaxReleaseStep keep their separate controls, while only the modified common Thermal config is materialized.
- **Boot/runtime verification is sparse-aware.** Changed files are verified against generated module output; untouched files are verified against the cached stock source.

## Pixel 11 support

| Device | Codename | Alpha9 policy |
| --- | --- | --- |
| Pixel 11 | `cubs` | Experimental · Stock polling/timing · Outdoor Safe up to +1 °C |
| Pixel 11 Pro | `grizzly` | Experimental · Stock polling/timing · Outdoor Safe up to +1 °C |
| Pixel 11 Pro XL | `kodiak` | Experimental · Stock polling/timing · Outdoor Safe up to +1 °C |
| Pixel 11 Pro Fold | `yogi` | Experimental · Stock polling/timing · Outdoor Safe up to +1 °C |

## Compatibility and safety

- Pixel 9/9a/10/10a keep the existing full three-file overlay contract; sparse materialization is Pixel 11 / Tensor G6 only.
- Pixel 11 classic PollingDelay and PassiveDelay controls are not exposed.
- pTune Thermal coexistence override remains blocked on experimental Pixel 11 targets.
- ZRAM, Emerald Hill and page-cluster experiments remain separately controlled and reversible where supported.
- The WebUI remains loopback-only and uses the same guarded typed control surface for standalone and embedded launch paths.

## Updating

Users already on the prerelease channel can update through the normal module update flow after the prerelease channel metadata is promoted. A reboot is required after flashing before judging runtime state.

## Credits

- **Harish / Codecity001** — Pixel 11 / Tensor G6 hardware testing, installer/update feedback and Thermal recovery review.
- **Allen Chang** — Canary/device evidence and Thermal-profile feedback.
- **marx161** — original project foundation and earlier module work.
- **Lycidias93** — integration, validation and release maintenance.
- Shared WebUI foundation: **[Android Root Module Standalone WebUI Template](https://github.com/Lycidias93/android-root-module-webui-template)**.
