# v1.5.2-universal-test.1 runtime evidence: mustang CP2A

Status: runtime PASS
Date: 2026-07-03
Device: mustang / Pixel 10 Pro XL
Build: CP2A.260605.012 / incremental 15430684
Version: 1.5.2-universal-test.1
VersionCode: 1016201

## Result

`1.5.2-universal-test.1` is runtime-confirmed on:

```text
mustang / Android 17 / CP2A.260605.012 / outdoor-extended / polling mod / ZRAM 100p
```

Runtime status observed after reboot:

```text
P:🟢 mod | T:🟢 outdoor-ext | Z:🟢 100p | Action: settings/debug
```

## Active profile

Because the runtime build is CP2A, the expected selected profile is:

```text
mustang-android17-cp2a-cp2a260605012-outdoor-extended
```

The CP31.260618.005 selector and alias checks still passed in the installed module, but the CP31 alias profile is not expected to be active on CP2A.

## Evidence summary

Observed PASS evidence:

- module active with no `disable`, `remove`, or `skip_mount`
- `module.prop` version `1.5.2-universal-test.1`
- `module.prop` versionCode `1016201`
- stable `update.json` remains `1.5.1-universal.1` / `1016108`
- CP31.260618.005 selection verify PASS
- CP31.260618.005 alias verify PASS
- CP31 profile-name hygiene audit PASS
- runtime evidence scope PASS
- profile matrix PASS, count `83`
- manager status P/T/Z green
- vendor overlay match
- reboot status safe
- ZRAM 100p enabled and runtime-visible

## Scope honesty

This is a runtime PASS for mustang on CP2A only.

It does not add runtime-proven claims for:

- frankel
- blazer
- rango

It also does not prove CP31 runtime on-device yet. CP31.260618.005 coverage remains factory-basis plus selector/alias verification until a CP31 runtime log is available.
